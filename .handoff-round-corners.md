# Handoff: 白色圆角方角层问题

## 问题描述

DeepSeekDesktopAssistant（Tauri 2 + WebView2）窗口四角圆角下方会出现白色半透明的方角层。CSS `border-radius: 12px` 裁剪了 body 的背景，但底层的 WebView2 原生渲染表面是白色矩形，不会被 CSS 裁剪。

---

## 当前状态

### 已尝试的方案（DWM 圆角裁剪 — 未生效）

**修改文件：**
- [src/src/windows.rs](src/src/windows.rs) — 在 `apply_win11_rounded_corners()` 中设置了 4 个 DWM 属性
- [src/src/lib.rs:151-153](src/src/lib.rs) — 在 setup 阶段调用 `apply_rounded_corners_to_all()`

```rust
DwmSetWindowAttribute(hwnd, 33, DWMWCP_ROUND);               // 圆角
DwmSetWindowAttribute(hwnd, 2, DWMNCRP_DISABLED);            // 禁用非客户区渲染
DwmSetWindowAttribute(hwnd, 38, DWMSBT_NONE);                // 抑制系统背景
DwmSetWindowAttribute(hwnd, 3, TRUE);                        // 禁用过渡效果
```

DWM 调用覆盖了以下窗口创建点：
- `lib.rs` setup 中预热的 "main"、"widget"、"trend-detail"（通过 `apply_rounded_corners_to_all`）
- `show_panel()` 中新建的 "main" 窗口
- `show_settings()` 中新建的 "settings" 窗口
- `show_widget()` / `init_widget()` 中新建的 "widget" 窗口

**结果：** 白色方角依然存在。

**可能原因：**
1. `hwnd()` 返回的句柄可能不是最顶层窗口句柄
2. WebView2 的 WebView 层有自己的窗口/渲染表面，DWM 属性可能作用在错误的句柄上
3. 对于 `transparent(true)` 的窗口，DWM 圆角属性可能被 WebView2 覆盖
4. `apply_rounded_corners_to_all` 在 setup 中调用时，窗口可能还没完全初始化

---

## 与两个 Raw 项目的对比

| | RAW_DeepSeekDesktopAssistant | RAW_MimoDesktopAssistant | 当前项目 |
|---|---|---|---|
| **body 背景** | `rgba(255,255,255,0.3)` + backdrop-filter | `rgba(255,255,255,0.3)` + backdrop-filter | `#fafafa` (opaque) |
| **html 背景** | `transparent` | `transparent` | `transparent !important` |
| **border-radius** | `8px` | `8px` | `12px` |
| **DWM 圆角** | **无**（根本没有这个函数） | **无**（根本没有这个函数） | 有但未生效 |
| **transparent** | `true` | `true` | `true` |
| **毛玻璃效果** | body 半透明 + blur | body 半透明 + blur | 仅 collapsed 状态下 |

**关键发现：** 两个 Raw 项目也设置了 `html { background: transparent }` 和 `body { border-radius: 8px }`，且 **没有使用任何 DWM 圆角处理**。它们的半透明毛玻璃效果理论上应该也会暴露白色底层，但用户说它们没有这个问题。

---

## 有用的参考文件

- **Raw 项目 style.css:** [RAW_DeepSeekDesktopAssistant/frontend/src/style.css](RAW_DeepSeekDesktopAssistant/frontend/src/style.css)
- **Raw 项目 windows.rs:** [RAW_DeepSeekDesktopAssistant/src/src/windows.rs](RAW_DeepSeekDesktopAssistant/src/src/windows.rs)
- **Raw 项目 lib.rs:** [RAW_DeepSeekDesktopAssistant/src/src/lib.rs](RAW_DeepSeekDesktopAssistant/src/src/lib.rs)
- **Raw Mimo style.css:** [RAW_MimoDesktopAssistant/frontend/src/style.css](RAW_MimoDesktopAssistant/frontend/src/style.css)
- **Raw Mimo windows.rs:** [RAW_MimoDesktopAssistant/src/src/windows.rs](RAW_MimoDesktopAssistant/src/src/windows.rs)
- **当前项目 style.css:** [frontend/src/style.css](frontend/src/style.css)
- **当前项目 windows.rs:** [src/src/windows.rs](src/src/windows.rs)
- **当前项目 lib.rs:** [src/src/lib.rs](src/src/lib.rs)

---

## 后续排查方向

### 方向 1：CSS 方案 — 让 html 背景匹配 body

这是最简单直接的方案。当前 `html { background: transparent !important; }` 暴露了 WebView2 的白色底层。改成：

```css
html { background: #fafafa; }
html.dark { background: #111111; }
#app { background: transparent !important; }
```

保留 `#app` 透明是为了避免 DWM 阴影（原始注释所述），让 html 不透明来遮住白色底层。

**验证方式：** 改完重新构建，观察四角是否还有白色。

### 方向 2：用 WebView2 自身的背景色 API

在 Rust 侧，创建 WebView 后调用 WebView2 的 `put_BackgroundColor` API 将 WebView2 底层设为透明或匹配的颜色。参考 `webview2-com` crate 的 `ICoreWebView2Controller` 接口。

### 方向 3：对比 Raw 项目找到差异

两个 Raw 项目都没有白色方角问题但结构类似，说明差异可能在：
- Tailwind 配置或 postcss 配置不同
- Vue 模板中对 `#app` 或根元素的不同处理
- index.html 中的 meta 标签或样式设置不同
- 窗口创建时的参数顺序（transparent 必须在 build 前设置等）
- 可能是开发环境与构建产物的行为差异

### 方向 4：调试 DWM 调用

在 `apply_win11_rounded_corners` 中检查 `DwmSetWindowAttribute` 的返回值（i32），确认 DWM 调用是否成功。同时打印 hwnd 的值确认窗口句柄有效。

---

## 构建与运行

- **Debug 构建：** `cd src && cargo build`
- **前端构建：** `cd frontend && npx vite build`
- **完整构建：** 运行 `build.bat`（生产构建，包含 NSIS 安装包）
- **启动：** `src/target/debug/DeepSeekDesktopAssistant.exe`

当前分支：`master`
